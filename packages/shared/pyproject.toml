[project]
name = "pixel-tools-shared"
version = "0.1.0"
description = "Internal shared helpers for pixel-mcp (AXI envelope, common types). Not published."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pixel_tools_shared"]
